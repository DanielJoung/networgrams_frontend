import React, { Component } from "react";

class LikeButton extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <>{/* <p>{this.state.post.like}</p> */}</>;
  }
}

export default LikeButton;
